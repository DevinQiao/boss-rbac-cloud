spring:
  application:
    name: Boss-Monitor-Admin
  cloud:
    nacos:
      config:
        server-addr: ${nacos.url}:9001
        group: DEFAULT_GROUP
        prefix: boss-monitor-admin
        file-extension: yaml